# ── Paste these into Vercel Dashboard → Project → Settings → Environment Variables ──

# PostgreSQL (Supabase) — get from Supabase Dashboard → Settings → Database → Connection string
DATABASE_URL=postgresql://postgres:[PASSWORD]@db.zyyenemgomarotjkuwgo.supabase.co:5432/postgres

# JWT Secret — generate with: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=replace_with_64_char_random_hex

# Frontend — set to your Vercel deployment URL after first deploy
VITE_API_URL=/api
